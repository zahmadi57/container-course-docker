apiVersion: v1
kind: Service
metadata:
  name: code-server
  namespace: code-server
  labels:
    app: code-server
spec:
  type: NodePort
  selector:
    app: code-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
